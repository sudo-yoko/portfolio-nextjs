import { OffsetFetcher } from '@/modules/(system)/pager/pager-model';
import { User, UsersQuery } from '@/modules/users/models/users-model';

const usersFetcher: OffsetFetcher<User, UsersQuery> = (user, query) => {
    
};
